# Assignment 2: Implementation of String function.
 
## Overview
 
This assignment involves outputting a string returned from a function call using std::cout. In the /src/Physical_properties.cpp file, a function named outputArea(float area)is defined, which returns a string. This string is then outputted to the user with std::cout.
 
## Function Definition
 
1. **File location**.
 
    `src\Physical_properties.cpp`

2. **Function signature**.
    - Returns a string which is mixture of output message and area
 
## Usage

1. **Function Call**
    - Call the `outputArea(float area)` function in the physical_properties.cpp file.

2. **Output to user**.
    - Area of 3D object is 6.
    